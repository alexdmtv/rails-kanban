<%= render 'shared/modal' do %>
  <div class="flex flex-col gap-y-6">
    <h2 class="text-heading-l text-red">Delete this board?</h2>
    <p class="text-body-l text-medium-grey">
      Are you sure you want to delete the ‘<%= @board.name %>’ board? This action will remove all columns and tasks and
      cannot be reversed.
    </p>
    <div class="flex gap-x-4">
      <%= link_to 'Delete', board_path(@board), data: { turbo_method: :delete, turbo_frame: '_top' }, class: 'destructive_sm_btn' %>
      <%= link_to 'Cancel', '#', data: { action: 'modal#close', turbo_frame: '_top' }, class: 'secondary_sm_btn' %>
    </div>
  </div>

<% end %>