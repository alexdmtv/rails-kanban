<div
  id="<%= dom_id(list) %>_header"
  data-reorder-path="<%= reorder_board_list_path(list.board, list) %>"
  data-lists-target="list"
  class="flex flex-col gap-6 w-[280px]">
  <div
    class="flex items-start gap-3"
  >
    <div
      class="h-4 w-4 flex-shrink-0 rounded-full bg-main-purple"
      style="background-color: <%= list.color_code %>"
    ></div>
    <h3 class="uppercase text-heading-s text-medium-grey"><%= list.name %></h3>
    <button
      class="ml-auto flex-shrink-0 hover:cursor-grab select-none touch-none"
      data-lists-target='listDragHandle'
    >
      <%= svg_tag 'icon-drag-handle', class: "h-6 w-6 text-medium-grey" %>
    </button>
  </div>

  <div
    id="<%= dom_id(list) %>_content"
    class="flex shrink-0 grow flex-col gap-5">
    <%= render list.tasks.sort_by { |task| task[:list_order] } %>
  </div>
</div>